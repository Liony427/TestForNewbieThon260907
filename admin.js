const testBtn = document.getElementById("testBtn");
const message = document.getElementById("message");

testBtn.addEventListener("click", function () {

    message.textContent = "Admin 페이지 JavaScript 정상 작동!";

});